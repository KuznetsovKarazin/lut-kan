[pytest]
testpaths = tests
python_files = test_*.py
markers =
    smoke: fast smoke tests
    numeric: numerical correctness tests
    regression: regression tests guarding previously verified behavior
